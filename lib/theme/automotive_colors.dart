import 'package:flutter/material.dart';

class AutomotiveColors {
  // Base backgrounds
  static const Color background = Color(0xFF0B0E14);      // Deep carbon dark
  static const Color cardBackground = Color(0xFF151A24);  // Elevated panel background
  static const Color cardBorder = Color(0xFF262E3D);      // Subtle border line
  static const Color surfaceGlass = Color(0x1F2A3447);    // Glassmorphism overlay

  // Accent colors
  static const Color cyanAccent = Color(0xFF00E5FF);     // Electric cyan for primary actions/glow
  static const Color blueAccent = Color(0xFF2979FF);     // CarPlay blue
  static const Color greenAccent = Color(0xFF00E676);    // Android Auto green / status OK
  static const Color orangeAccent = Color(0xFFFF9100);   // Warnings / indicators
  static const Color redAccent = Color(0xFFFF1744);      // Alerts / engine rev limit
  static const Color purpleAccent = Color(0xFFD500F9);   // Media accent

  // Text & Icons
  static const Color textPrimary = Color(0xFFF1F5F9);    // High contrast white/slate
  static const Color textSecondary = Color(0xFF94A3B8);  // Subdued labels
  static const Color textMuted = Color(0xFF64748B);      // Dimmed details
  
  // Projection Brand Colors
  static const Color carPlayColor = Color(0xFF007AFF);   // Apple CarPlay Signature Blue
  static const Color androidAutoColor = Color(0xFF34A853);// Android Auto Signature Green
}
